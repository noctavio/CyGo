<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color of the rectangle (red) -->
    <solid android:color="#FF0000"/>

    <!-- Rounded corners -->
    <corners android:radius="30dp"/>

    <padding
        android:left="5dp"
        android:right="5dp"
        android:top="5dp"
        android:bottom="5dp"/>

</shape>